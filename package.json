{
  "name": "nomad-content-platform",
  "private": true,
  "version": "1.0.0",
  "description": "Plataforma de aprovação de conteúdo da agência NOMAD — microserviços + frontend React",
  "workspaces": [
    "frontend",
    "services/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n auth,posts,files,gateway,web -c green,magenta,yellow,blue,cyan \"npm:dev:auth\" \"npm:dev:posts\" \"npm:dev:files\" \"npm:dev:gateway\" \"npm:dev:web\"",
    "dev:gateway": "npm run dev -w services/gateway",
    "dev:auth": "npm run dev -w services/auth",
    "dev:posts": "npm run dev -w services/posts",
    "dev:files": "npm run dev -w services/files",
    "dev:web": "npm run dev -w frontend",
    "start": "concurrently -k -n auth,posts,files,gateway -c green,magenta,yellow,blue \"npm:start -w services/auth\" \"npm:start -w services/posts\" \"npm:start -w services/files\" \"npm:start -w services/gateway\"",
    "build": "npm run build -w frontend",
    "reset": "node scripts/reset.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
